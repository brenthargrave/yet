{
  "extends": "ts-node/node16/tsconfig.json",
  "ts-node": {
    // Remove if you want ts-node to do typechecking.
    // "transpileOnly": true,
    "files": true,
    "compilerOptions": {
    }
  },
  "compilerOptions": {
    "lib": ["dom", "esnext"],
    "target": "esnext",
    "strict": true,
  }
}
