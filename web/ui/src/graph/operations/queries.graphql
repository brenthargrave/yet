query GetEvents {
  events {
    name
    anonId
  }
}

query Me {
  me {
    id
    token
    e164
    name
    org
    role
  }
}

query Contacts {
  contacts {
    id
    name
    org
    role
  }
}
