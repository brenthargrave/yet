mutation CreateVerification($input: CreateVerificationInput!) {
  createVerification(input:$input) {
    successful
    messages {
      code
      field
      message
    }
    result {
      status
    }
  }
}
