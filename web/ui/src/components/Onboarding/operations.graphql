mutation UpdateProfile($input: ProfileInput!) {
  updateProfile(input: $input) {
    success
    me {
      id
      name
      org
      role
      e164
      token
    }
    userError {
      message
    }
  }
}
